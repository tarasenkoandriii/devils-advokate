# Аудит документации и TODO — 2026-08-30

Проверка тем же принципом, что и аудит кода: не читать и доверять, а проверять
исполнением/скриптом. Автоматическая сверка всех файловых ссылок в `../README.md`,
`../TODO.md`, `../VERCEL.md`, `apps/api/prisma/README.md` против реальной файловой
системы, плюс перекрёстная сверка `docs/*.md`.

## Найдено и исправлено

### 1. VERCEL.md — таблица env-переменных API отставала на 11 секретов (серьёзно)
Сверка со `.env.example` показала: в гайде по деплою не было `API_PUBLIC_BASE_URL`,
`ASSEMBLYAI_API_KEY`, `ASSEMBLYAI_WEBHOOK_SECRET`, `SCHEDULER_DISPATCH_SECRET`,
`ELEVENLABS_API_KEY`, `GOOGLE_PLACES_API_KEY`, `GOOGLE_VISION_API_KEY`, `SERPAPI_KEY`,
`VERCEL_BLOB_READ_WRITE_TOKEN`, `FACT_CHECK_TOOLS_API_KEY`, `YOUTUBE_API_KEY`. Деплой по
старой версии таблицы прошёл бы успешно, но тихо сломал бы доказательства ДТП, OCR
анализов, TTS, разбор медиа, поиск по инвест-источникам, **вебхуки транскрипции целиком**
(fail closed по недавнему аудиту) и все три pg_cron-задания. Таблица переписана полностью,
с комментарием к каждой переменной, откуда она и что сломается без неё.

### 2. VERCEL.md — раздел про `manual-migrations/` отсутствовал
`prisma migrate deploy` не покрывает то, что не выражается в схеме: индексы/Decimal из
дневного аудита БД, три pg_cron-задания. Добавлен раздел с перечнем файлов и порядком
применения.

### 3. Два битых относительных пути
`../README.md` ссылался на `prisma/manual-migrations/intake_session.sql` (нет такого от
корня репозитория — должно быть `apps/api/prisma/...`); `../VERCEL.md` — на `prisma/README.md`
вместо `apps/api/prisma/README.md`. Оба исправлены.

### 4. TODO.md — три раздела описывали уже закрытые вопросы как открытые
- «Мёртвая схема Prisma — 5 моделей, решение не принято» → решение принято и выполнено
  сегодняшним аудитом БД (точная цифра 7, не 5; удалены 5, `SteelmanSupportingFact`
  сохранена как ложное срабатывание, `AIModel` — как используемая). Раздел переписан
  на факт с ссылкой на `AUDIT-DB-2026-08-30.md`.
- «Неиспользуемые функции venue — вероятно намеренно, не требует действия» → по итогам
  сегодняшнего полного аудита решение изменилось, функции удалены вместе с семью другими.
  Раздел переписан.
- «`request.userRestricted` не проверяется ни одним write-эндпоинтом» → проверено по
  коду: `NotRestrictedGuard` стоит на 9 эндпоинтах, включая все три названных в исходном
  ТЗ примером. Раздел помечен закрытым.

### 5. README.md — историческая запись про 11–13 моделей дополнена указателем вперёд
Сама историческая формулировка (что нашёл аудит MVP v4) не тронута — добавлена
парентеза «решение принято и выполнено аудитом БД 2026-08-30», чтобы читатель не
следовал по цепочке к устаревшему выводу.

### 6. apps/api/prisma/README.md — закрывающая запись к «Чекпоинту 1»
Раздел в начале файла документирует факт-систему `FactAssertion`/`Observation`/
`AIInferenceSource`, которая была спроектирована, затем незаметно вытеснена
`PersonFact`/`ConversationSignal`/`FactSource` и сегодня удалена аудитом БД. Добавлена
запись в конец журнала (по конвенции файла — append-only), поясняющая, что случилось
с моделями из тех разделов, без переписывания самой историю.

## Проверено — расхождений не найдено
- Все файловые пути в `../README.md`/`../TODO.md`/`../VERCEL.md`/`apps/api/prisma/README.md`
  существуют (после исправлений §3).
- Markdown-разметка (парность backtick-ов) не повреждена правками.
- 4 файла в `docs/` не связаны прямой ссылкой ни с одним другим документом
  (`features-map-brief.md`, `how-to-use-brief.md`, `illustration-vs-ai-wrapper.md`,
  `illustration-vs-surveillance.md`) — проверено: это не битые ссылки, а
  последовательный для всей серии «бриф → спека» паттерн (каждый бриф ссылается на
  родительскую спеку, ни одна спека не ссылается вниз на брифы; тот же паттерн у
  `illustration-vs-calculators.md`, которую не пометило орфаном только потому, что на
  неё случайно сослался соседний бриф). Не дефект, оставлено как есть.
- `../TODO.md` не имеет оглавления — не нужно, это плоский список датированных записей,
  тот же формат, что и остальной проект.
- Число «93 модели» в историческом абзаце README (аудит MVP v4) оставлено как есть —
  это точный снимок на момент ТОГО аудита, тот же принцип, что соседние записи
  «269 тестов» / «855 тестов», не текущее утверждение.
